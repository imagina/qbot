export const SET_DEFAULT_MENU = (state, data) => {
  state.defaultMenu = data
};

export function SET_MENUS(state, payload) {
  state.menus = payload
}

export function SET_MENU_ITEMS(state, payload) {
  state.menuItems = payload
}

